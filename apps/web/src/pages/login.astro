---
import { actions } from "astro:actions";
import { AuthRoot } from "../lib/auth/oauth/auth-root";

const result = Astro.getActionResult(actions.loginWithBluesky);
console.log("error", result?.error);
---

<AuthRoot client:only />
