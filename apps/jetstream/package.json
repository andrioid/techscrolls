{
  "name": "jetstream",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch --env-file=../../.env index.ts",
    "prod": "bun index.ts",
    "prod:node": "node --experimental-strip-types index.ts",
    "prod:deno": "deno run --allow-env --allow-read --allow-ffi --allow-net --unstable-sloppy-imports --env-file=../../.env index.ts"
  },
  "dependencies": {
    "@andrioid/atproto": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
